{
  "name": "msgpack-to-json-utools",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build",
    "test": "node --test test/*.test.js",
    "check": "npm test && npm run build"
  },
  "dependencies": {
    "@msgpack/msgpack": "3.1.3"
  },
  "devDependencies": {
    "vite": "8.2.2"
  }
}
